{
  "name": "super-auto-men",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run test:e2e -w @sam/app",
    "format": "prettier --write .",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @sam/app",
    "preview": "npm run preview -w @sam/app",
    "sim": "tsx tools/simcli/src/main.ts",
    "test:update-golden": "cross-env UPDATE_GOLDEN=1 vitest run --project @sam/content golden"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.4.1",
    "cross-env": "10.1.0",
    "eslint": "10.10.0",
    "globals": "17.12.0",
    "prettier": "3.9.6",
    "tsx": "4.23.13",
    "typescript": "6.0.3",
    "typescript-eslint": "8.69.0",
    "vitest": "5.0.0"
  }
}
